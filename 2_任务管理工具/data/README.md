# 日序本地数据

此目录存放当前电脑上的任务、习惯、排序、置顶、状态历史和桌面配置。

- `rixu-data.json`：可读的数据主副本。
- `rixu-data.js`：供本地网页启动时读取的同步镜像。
- 两个文件由日序自动维护，不应手动修改。
- 数据文件已被 Git 忽略，因此创建分支、提交源码或制作开发版本时不会自动打包个人数据。
- 迁移个人数据时，可单独复制这两个文件；只迁移项目代码时不要复制它们。
